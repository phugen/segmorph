#include "activeContour.hpp"

// apply the "active contours" algorithm to an image.
// Tries fitting a bunch of control points that define a
// spline to an image so that "external and internal energy" are minimized.
/*cv::Mat segmentSnake(cv::Mat input)
{
	
}*/